#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define speedup cin.tie(0), cout.tie(0), ios::sync_with_stdio(0)

const ll maxm = 1e5 + 5, maxn = 505;
const ll inf = 0x3f3f3f3f3f3f3f3f;
ll n, m;

int main()
{
	speedup;

	cin >> n >> m;
	for (ll i = 0; i < m; ++i)
	{
		ll t, a, b, c;
		cin >> t >> a >> b >> c;
	}

	return 0;
}
